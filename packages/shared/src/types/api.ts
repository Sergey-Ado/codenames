export enum Pages {
  WELCOME = '/',
  LOGIN = 'login',
  REGISTER = 'register',
}

export enum Endpoints {
  BASE = '/',
  USER = '/api/user',
}

export enum HttpStatus {
  OK = 200,
  BAD_REQUEST = 400,
  NOT_FOUND = 404,
}
