{
  "extends": ["//"],
  "tasks": {
    "build": {
      "env": [
        "PORT",
        "SHOW_LOG",
        "NODE_ENV",
        "JWT_SECRET_KEY",
        "FRONTEND_URL",
        "DATABASE_URL"
      ]
    }
  }
}
